server:
  host: 127.0.0.1
  port: 8000
  reconnectDelay: 1000
  reconnectEnabled: true
  reconnectInterval: 5000
  reconnectMaxAttempts: 0        # 0 = infinite attempts
  reconnectBackoff: true
  reconnectMaxDelay: 30000

log:
  level: info