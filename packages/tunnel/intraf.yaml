# Tunnel Server Configuration (Placeholder)

# Server configuration
server:
  host: "0.0.0.0"
  port: 9000  # Different from control server (8000)

# Control server connection
control:
  host: "127.0.0.1"
  port: 8000

# Logging
log:
  level: "info"
