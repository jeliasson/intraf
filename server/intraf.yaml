# Intraf Configuration File
# This file supports server, client, and common settings

# Server configuration
server:
  host: 0.0.0.0
  port: 8000

# Client configuration
client:
  serverUrl: ws://127.0.0.1:8000
  reconnectDelay: 1000

# Common configuration
logLevel: info
